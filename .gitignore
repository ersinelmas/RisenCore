# ===================================================================
# Standard Files and Directories to Ignore
# ===================================================================

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws
out/ # IntelliJ's default output directory

# Eclipse
.classpath
.project
.settings/
.apt_generated
.factorypath
.springBeans # For older Spring Tool Suite versions

# NetBeans
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/ # General build directory, NetBeans might use this
!**/src/main/**/build/
!**/src/test/**/build/

# VS Code
.vscode/

# STS (Spring Tool Suite 4)
.sts4-cache/ # Note: Added trailing slash for directory

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/wrapper/maven-wrapper.jar # Maven wrapper jar, should be re-downloaded by mvnw script

# Exceptions for Maven wrapper (if needed, usually not necessary to commit these)
# If you decide to commit mvnw files but not the jar itself
# !.mvn/wrapper/maven-wrapper.properties

# Compiled class files (generally covered by IDE/build tool specific ignores)
*.class

# Log files
*.log
*.log.*

# Package Files (usually handled by 'target/' or 'build/' from build tools)
# *.jar # Executable jars (like Spring Boot's) might be part of 'target/'
# *.war
# *.nar
# *.ear
# *.zip
# *.tar.gz
# *.rar

# Virtual machine crash logs
hs_err_pid*
replay_pid*

# Operating System Files
.DS_Store
Thumbs.db

# ===================================================================
# Project Specific & Sensitive Files
# ===================================================================

# RisenCore Specific (Add any project-specific temp files or configs here)

# Example for local sensitive configuration (BEST PRACTICE)
# If you create a local properties file for development with real passwords,
# uncomment and use it. Create an example file to commit instead.
# application-local.properties
# .env

# ===================================================================
# Files from Spring Initializr's .gitignore (usually help/documentation)
# ===================================================================
# HELP.md # This is a file generated by Spring Initializr, usually contains helpful links.
          # You might want to keep it if it's useful, or ignore it if not.
          # If you want to ignore it, uncomment the line below.
# HELP.md

# ===================================================================
# KEEP THESE FILES (Do NOT ignore Maven Wrapper scripts)
# These allow others to build the project without installing Maven locally.
# ===================================================================
!mvnw
!mvnw.cmd
!/.mvn/wrapper/maven-wrapper.properties